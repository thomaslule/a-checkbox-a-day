machine:
    ruby:
        version: 2.2.0

dependencies:
    override:
        - bundle install:
            timeout: 240
            pwd:
                test/acceptance

test:
    override:
        - ruby test/acceptance/smoke_test.rb
